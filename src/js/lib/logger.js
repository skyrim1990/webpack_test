export function Logger(loc){
	/*
	var that = this;
	var body = document.getElementById(loc);	
	var data = [];

	var print = function(message){
		that.data.push(message);
		that.flush();
	}
	
	var flush = function(){
		var tmp = "";
		for(let i = 0; i< data.length; i++){
			tmp += ""  + data[i];
		}
		body.innerHTML = tmp;
	}
	*/
}
